// const imageBasePath =
//   process.env.NODE_ENV === "development" ? `http://localhost:6038` : `https://api.le-fabre.com`; // live

// const imageBasePath = `https://api.zerozb.me`;
const imageBasePath = `http://192.168.0.242:4317`;

export default imageBasePath;
